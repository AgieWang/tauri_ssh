## 1. 确认、基线与现有工作映射

- [x] 1.1 审核并确认 14 份能力规格，在开始实施前记录已批准的调整。
- [x] 1.2 将 `add-team-knowledge-base-rag` 的 120 项任务映射为可复用、部分复用、已替代或仍缺失；只复用业务规则、数据、API、基础设施和仍符合新流程的测试证据，不复用旧 UI 交互设计或快照断言。
- [x] 1.3 重新读取实际工作区、当前分支、最新 `SCHEMA_VERSION`、知识库路由、Command 注册、API/类型与聚焦测试；在不修改无关进行中工作的前提下记录与本设计的差异。
- [x] 1.4 整理当前公共 Command/JSON/TypeScript 契约矩阵，覆盖 `versionStrategy` 等默认值、可选字段、枚举、错误及浏览器开发接口对等性。
- [x] 1.5 构建包含项目、发布版本、来源、文档、片段、向量、关系、任务和代码快照的当前数据库测试夹具，确保不含秘密或个人数据。
- [x] 1.6 记录项目/来源增删改查、文档浏览、全文检索、来源同步、向量生命周期、代码分析、检索和打开引用的业务结果与 API 基线；明确排除旧 UI 的布局、步骤、字段顺序、组件和文案基线。
- [x] 1.7 为目录、文档、异构解析、搜索、分析、图谱、向量和问答定义功能开关及发布阶段名称。
- [x] 1.8 从能力规格和普通用户任务独立固化六条全新主流程、中文业务术语和字段分级表，明确每个配置项属于必填、推荐默认、按需确认或高级设置，不从现有知识页面反推交互。

## 2. 技术验证与依赖决策

- [x] 2.1 在 macOS 上评估 DOCX、XLSX、PPTX 与文本型 PDF 的 Rust 原生或隔离解析方案，覆盖许可证、包体、内存、损坏文件和提取质量。
- [x] 2.2 使用脚本、事件处理器、外部资源、危险网址和超大 DOM 攻击夹具评估 HTML 与 SVG 清洗库。
- [x] 2.3 使用中文截图、中英文/代码标签、离线资源、延迟、内存和置信度输出评估本地 OCR 候选，并使用本机回环测试服务评估远程视觉接口契约。
- [x] 2.4 使用中文标题、版本号、路径、类名和接口路由，对比普通索引与独立全文表的标题精确、前缀和包含搜索性能。
- [x] 2.5 在返回 1000 与 5000 个节点时评估图谱界面候选方案的包体、Canvas/SVG 性能、键盘导航、缩放、焦点和证据选择。
- [x] 2.6 使用代表性团队数据校准单文件、批次、ZIP 展开、嵌套条目、处理时间、资产配额、图谱规模和查询延迟默认限制。
- [x] 2.7 在 `design.md` 或用户明确要求的后续产物中记录解析器、OCR、标题索引、图谱渲染器和限制值决策，评审前保持“待确认”状态。
- [x] 2.8 仅在 macOS 编译与安全检查通过后锁定依赖，并记录可选依赖不可用时的功能表现。

## 3. 领域模块骨架与 IPC 契约

- [x] 3.1 创建目录、文档、接入、搜索、分析、图谱、问答、任务和治理的 Rust 知识领域模块，暂不迁移现有行为。
- [x] 3.2 将知识模型拆分到领域文件，并保留现有聚合导出，确保当前调用方无需修改即可编译。
- [x] 3.3 为项目仓库、项目版本、文档草稿/上传、搜索、分析草稿、图谱查询、问答和任务定义请求、响应与错误契约表。
- [x] 3.4 为每个新 IPC DTO 增加明确的 serde 命名、默认值和可空规则及往返测试，覆盖缺失、空值、未知枚举、时间与标识边界。
- [x] 3.5 按领域拆分 TypeScript 类型和 API 模块，同时保留 `src/types` 与 `src/lib/api` 聚合导出。
- [x] 3.6 实现兼容门面，将现有 `KnowledgeService` 方法委托给新服务且不复制业务逻辑。
- [x] 3.7 增加契约测试，证明旧 Command 名称和参数仍映射到兼容门面并返回旧 JSON 结构。
- [x] 3.8 端到端注册新 Command 与 API 导出，验证 Command 定义、`generate_handler!`、开发接口对等、TypeScript 调用和结构化错误。

## 4. SQLite 结构、迁移与数据访问基础

- [x] 4.1 重新读取最新数据库结构并预留连续迁移版本，不修改或重排历史迁移。
- [x] 4.2 增加项目仓库绑定表与项目版本仓库清单表，明确主键、唯一性、软删除语义和查询索引。
- [x] 4.3 增加内容寻址资产元数据、文档草稿、文档版本绑定、分析运行/草稿及必要解析产物表。
- [x] 4.4 增加作为派生数据的图谱节点、关系边与构建/投影表，并定义版本范围唯一性及证据引用。
- [x] 4.5 增加技术验证选定的标题搜索/回填及可观测/评测元数据结构。
- [x] 4.6 为各新表族实现参数化数据访问与事务方法，使用显式列、稳定行映射且不跨异步等待持锁。
- [x] 4.7 增加空库、上一版本、含数据的 v36/当前版本、回填失败、重试、未来版本拒绝和数据保留迁移测试。
- [x] 4.8 增加孤立资产、草稿父版本、版本绑定、项目清单、图谱证据、活动配置和现有引用标识完整性检查。
- [x] 4.9 使用代表性本地数据副本验证迁移和数据访问行为，保持原数据库不变并记录迁移前后数量。

## 5. 项目与多仓库目录

- [x] 5.1 将项目增删改查规则迁入目录 Service/Database 模块，保留稳定项目标识、别名、软删除和审计行为。
- [x] 5.2 实现多个已登记 Git 工作区的批量关联，包含原子冲突处理、仓库别名/角色/默认分支和幂等更新。
- [x] 5.3 使用只读 Git 参数白名单探测仓库可用性和未提交状态，不执行检出、暂存、重置、清理、拉取、推送、Hook 或脚本。
- [x] 5.4 实现仓库解除关联，停止后续同步并保留版本清单、文档、引用和审计历史。
- [x] 5.5 增加目录 Command、类型化 API 及重复名称、已移除工作区、重复绑定、批量局部失败、脏仓库和解除关联历史测试。
- [x] 5.6 构建使用简短中文标签的项目列表与概览界面，空状态直接提供“创建项目”主操作，并覆盖加载、错误、编辑、停用、删除和稳定导航。
- [x] 5.7 构建“创建项目 → 选择仓库 → 选择版本 → 确认同步”的线性引导，提供可搜索 Git 工作区多选和安全默认值，将仓库角色及逐仓库规则收纳到高级设置。
- [x] 5.8 使用真实浏览器验证普通用户无需理解内部标识或 Git 命令即可完成项目和多仓库流程，再通过真实 Tauri IPC 验证同一数据，不以模拟证据替代。

## 6. 资产存储与受管文档生命周期

- [x] 6.1 实现应用数据目录中的内容寻址资产存储，包含规范化文件名、原子复制、哈希校验、权限、引用计数和安全临时清理。
- [x] 6.2 实现人工文档创建和草稿保存，校验标题/类型/项目，使用乐观修订标识并从正式搜索与问答中排除草稿。
- [x] 6.3 实现创建不可变版本、父版本、作者/时间/说明、内容哈希、当前版本指针和索引任务的文档提交事务。
- [x] 6.4 实现文件上传意图、后端路径复核、资产复制、导入任务创建、进度、取消和逐文件批次结果。
- [x] 6.5 实现带权限过滤的文档元数据、详情和历史查询，并明确处理中、部分成功和解析失败状态。
- [x] 6.6 实现软删除、恢复、索引失效/重建、引用历史保留和独立永久删除影响预览，默认不启用永久删除。
- [x] 6.7 实现 Markdown/文本及解析后 Office 内容的版本比较，包括资产哈希和解析器版本变化。
- [x] 6.8 实现恢复为新草稿/版本及乐观并发冲突响应，并提供当前内容用于比较。
- [x] 6.9 增加创建、草稿、并发保存、提交、上传、查询、软删除、恢复、比较和资产复制失败回滚的 Database/Service/IPC 测试。
- [x] 6.10 构建简化的文档新增与上传界面，从当前上下文预填项目/版本并推断标题/类型，仅展示需要确认的字段，失败或返回上一步时保留输入和已选文件。

## 7. 异构文档接入

- [x] 7.1 将现有 Markdown/文本/JSON/YAML/SQL/日志解析器重构到解析器注册表后，保持已验证输出不变。
- [x] 7.2 实现共享的文件签名、MIME、扩展名、大小、数量、解压、嵌套、超时和解析能力校验。
- [x] 7.3 实现经过清洗的 HTML/HTM 解析器，提取可见文本、标题、控件、相对资源引用和来源位置，且不执行或获取外部内容。
- [x] 7.4 实现 DOCX 标题、段落、列表、表格和图片引用解析，并对不支持对象给出警告。
- [x] 7.5 实现 XLSX 工作表、单元格、表格、命名区域、公式、缓存值和稳定工作表/单元格引用解析，且不执行公式。
- [x] 7.6 实现 PPTX 幻灯片顺序、文本、备注、表格和图片引用解析，并保留稳定页码引用。
- [x] 7.7 实现 PDF 文本层解析与扫描页检测，对纯图片页面返回明确的“需要 OCR”状态。
- [ ] 7.8 实现图片元数据/预览接入及选定的本地 OCR 适配器；远程视觉使用独立授权适配器。
- [ ] 7.9 在未配置安全转换器时为旧版 DOC/XLS/PPT 返回明确能力错误，并在隔离且不执行宏的路径中验证转换器。
- [ ] 7.10 增加有效、部分成功、损坏、错误扩展名、超限、加密、类似压缩炸弹、含脚本、符号链接、Unicode 文件名和已取消输入的真实夹具。
- [ ] 7.11 验证批量局部失败、临时清理、解析质量/警告、稳定引用，并确保仅成功解析版本进入索引。

## 8. 标题与全文搜索

- [ ] 8.1 实现规范化标题索引与回填，支持精确、前缀和包含匹配，并优先展示标题精确匹配结果。
- [ ] 8.2 调整全文检索索引，使其仅索引已提交的当前文档版本，并保留 Office 工作表、表格、幻灯片和 HTML 内容块的结构位置。
- [ ] 8.3 在排序前对项目、仓库、项目版本、快照、文档类型、来源、状态、标签和权限执行硬过滤。
- [ ] 8.4 实现稳定的游标与分页语义、结果快照与变更提示，以及标题和全文检索通道间的去重。
- [ ] 8.5 实现可解释的结果通道、排序摘要、安全高亮和引用详情链接。
- [ ] 8.6 实现可选的向量和已确认图谱增强，同时保证这些通道不可用时标题与全文检索仍可独立工作。
- [ ] 8.7 增加覆盖中文标题、版本号、路径、精确标识符、Office 表格文本、无 OCR 图片、已删除或草稿文档、权限过滤和跨版本排除的搜索测试。
- [ ] 8.8 对标题、全文检索和过滤分页进行基准测试，记录目标语料规模下的 P50/P95、候选数、数据库执行计划、内存和结果稳定性。
- [ ] 8.9 构建以单一搜索框为默认入口的搜索界面，继承项目与版本上下文，将仓库、类型、来源、状态和通道筛选折叠为高级筛选，并以中文业务摘要展示命中、降级和引用。

## 9. 项目版本清单与自定义文档版本

- [x] 9.1 支持通过 Tag、分支或 Commit 创建项目版本，为每个仓库记录请求引用及解析后的不可变 Commit SHA。
- [x] 9.2 支持同名 Tag 批量映射、逐仓库覆盖和显式排除，并阻止映射不完整的版本进入就绪状态。
- [x] 9.3 分支重新采集时创建新的不可变清单，不修改此前已解析的 Commit。
- [ ] 9.4 将来源同步、自定义、上传、生成及代码分析文档版本绑定到项目版本或明确的跨版本范围。
- [ ] 9.5 禁止将未绑定的自定义文档静默归入最新项目版本。
- [ ] 9.6 计算项目版本在仓库采集、文档同步、解析、索引、分析、图谱和向量阶段的完整度。
- [ ] 9.7 增加多仓库清单测试，覆盖共享 Tag、Tag 缺失、混合引用、分支移动、脏工作区、排除仓库和跨版本同名实体。
- [ ] 9.8 构建线性版本管理界面，优先推荐所有仓库共同存在的 Tag 或默认分支，只在差异仓库出现时展开逐仓库处理，并将 Commit SHA 与覆盖规则放入高级详情。
- [ ] 9.9 验证历史搜索和文档比较不会使用后续版本证据，除非用户明确选择跨版本比较。

## 10. 只读代码分析与可审核 AI 文档

- [ ] 10.1 将 Git 快照读取收口到 `GitSnapshotReader`，使用 NUL 分隔路径、安全解码 Git 路径、无需检出即可读取对象，并保持脏工作区隔离。
- [ ] 10.2 将静态文件、代码元素和关系提取重构为分析器，并明确展示 `ast`、结构化降级、纯文本和已跳过等质量等级。
- [ ] 10.3 在发起任何 AI 请求前，生成确定性的仓库、模块、API/IPC、数据库、配置、测试及跨仓库依赖证据。
- [ ] 10.4 实现分析运行记录，冻结项目清单、仓库 Commit、包含/排除规则、语言、分析器版本和证据哈希。
- [ ] 10.5 仅使用获准证据构建本地或远程 AI 上下文，提供内容预览、服务商能力过滤和大小限制，且不执行仓库代码。
- [ ] 10.6 实现带服务商、模型、模板和证据来源的 AI 分析草稿，逐项验证主张引用，区分事实与解释，并拒绝无引用主张。
- [ ] 10.7 实现草稿查看、编辑、差异比较及确认入库事务；确认时创建不可变的人工确认文档版本，并保留原始 AI 草稿。
- [ ] 10.8 增加覆盖 Tag 分析、脏工作区标记、多仓库证据、中文及转义路径、`.codex/**`、二进制或跳过文件、动态调用、本机测试 Provider 远程拒绝、无效引用和编辑后确认的测试。
- [ ] 10.9 构建分析界面，支持快照选择、范围展示、防重复点击、进度、局部失败、证据预览、草稿编辑、差异比较和确认入库。
- [ ] 10.10 使用本机多仓库夹具和可控测试 Provider 执行完整分析，在不改变任何工作区的情况下验证确认后的文档可被搜索，并精确引用 Commit、路径和行号证据。

## 11. 版本化知识图谱

- [ ] 11.1 为项目、仓库、版本、文档、章节、代码元素、API、数据库对象、测试和支持的开发实体定义稳定的图谱实体键及关系类型注册表。
- [ ] 11.2 基于现有知识、来源和代码关系构建图谱投影，不改变来源关系事实。
- [ ] 11.3 按项目版本、来源哈希、关系哈希和投影版本建立增量图谱构建任务，并支持蓝绿激活。
- [ ] 11.4 实现明确的版本隔离和跨版本演进边，不得仅根据名称标签合并同名实体。
- [ ] 11.5 按根节点、方向、关系类型、确认状态、深度及节点/边数量限制执行有界子图查询，并返回稳定证据链接。
- [ ] 11.6 将 AI 关系建议保存为未确认边，并复用或扩展现有的显式关系确认审计流程。
- [ ] 11.7 增加覆盖重复抑制、证据缺失、重建失败回滚、跨版本隔离、置信度/确认状态和遍历限制的图谱完整性测试。
- [ ] 11.8 构建图谱界面，默认聚焦当前实体及一层已确认关系，使用中文业务标签和“继续展开/查看证据”操作，将深度、方向、置信度和未确认关系筛选收纳到高级设置，同时覆盖缩放、键盘、加载/空/错误/超限状态。
- [ ] 11.9 使用真实同步文档和已分析代码验证图谱查询，包括从所选关系边重新打开精确证据。

## 12. 本地与远程向量化集成

- [ ] 12.1 调整现有向量化配置指纹，使其包含解析器、规范化、分块、项目版本绑定、服务商能力、模型修订版、维度、前缀和归一化设置。
- [ ] 12.2 将向量和当前生效配置元数据保存在本地 SQLite，并通过本机测试 Provider 验证远程服务商契约仅用于计算向量、不持久化知识数据。
- [ ] 12.3 将已提交文档版本及图谱/搜索元数据纳入增量向量构建资格和复用规则。
- [ ] 12.4 在模块重构期间保留蓝绿重建、校验、原子激活、保留、回滚和失败构建隔离能力。
- [ ] 12.5 每次远程重建前，强制执行系统、来源、文档和内容授权，并展示预计文档数与字符数。
- [ ] 12.6 本地向量化失败时不得自动调用远程向量化，并提供可操作的本地运行时或模型状态。
- [ ] 12.7 使用本机回环测试 Provider 增加配置、序列化和授权测试，覆盖同维度不同模型拒绝、意外维度、远程部分响应、重试、取消、回滚和敏感内容阻断。
- [ ] 12.8 对测试语料执行本地向量化，验证向量本地存储、配置切换/回滚行为及离线标题/全文检索可用性；不要求连接真实远程向量服务。
- [ ] 12.9 构建“本地推荐/远程自定义”两步式向量配置界面，自动检测并预填安全值，将维度、前缀、归一化、分块和模型修订收纳到高级设置，并用中文说明检查结果和处理建议。

## 13. 基于证据的项目问答

- [ ] 13.1 在检索前解析项目、版本和仓库范围，对歧义返回候选项并强制停止后续处理。
- [ ] 13.2 将标题、全文检索、当前向量和有界已确认图谱通道整合为统一检索结果，并保留通道来源及版本过滤信息。
- [ ] 13.3 构建可检查的上下文，使用稳定引用标识、最小必要摘录、权限/敏感度过滤及令牌/字符限制，且不包含凭证数据。
- [ ] 13.4 按真实对话能力筛选问答服务商，使仅支持向量化的服务商不会出现在对话选项中。
- [ ] 13.5 生成包含范围、结论、实现、测试、风险、缺口/冲突、后续演进和引用的结构化回答。
- [ ] 13.6 实现纯证据主张验证、证据不足拒答、冲突展示、失败测试处理和跨版本泄漏防护。
- [ ] 13.7 增加问答回归数据集，覆盖精确标识符、自然语言描述、项目歧义、版本缺失、文档冲突、测试失败、后续版本变化、敏感片段和无向量降级。
- [ ] 13.8 构建直接提问的问答界面，默认继承项目/版本并选择推荐对话服务商，仅在歧义时要求补选；服务商切换、上下文预览和检索诊断按需展开，同时保留运行/取消、引用、缺口、冲突和证据打开操作。
- [ ] 13.9 使用本地模型或可控测试 Provider 对已同步且已向量化的内容执行完整问答，验证每项核心主张都能打开预期文档或 Commit、路径、行号证据，并将不受支持的问题记录为拒答。

## 14. 持久化任务编排

- [ ] 14.1 将现有知识任务处理重构为类型化执行器注册表，共用持久化状态机和已脱敏的载荷引用。
- [ ] 14.2 为同步、上传/解析、分析、向量、图谱和回填任务定义幂等键、检查点、可取消边界、清理、结果完整性和重试策略。
- [ ] 14.3 实现原子级重复抑制，使重复点击返回当前活动任务或明确冲突，而不是启动重复工作。
- [ ] 14.4 实现心跳、心跳丢失检测、中断恢复、安全检查点复用和应用启动恢复摘要。
- [ ] 14.5 实现协作式取消和逐文件/逐批次局部结果，不回滚此前已经提交的成功项。
- [ ] 14.6 针对速率限制、临时文件问题、数据库忙、解析器故障、模型失败和不可重试的校验错误，实现重试分类及有界退避。
- [ ] 14.7 桌面 Command 与浏览器开发 API 通过同一 Service 推送或轮询进度，使用中文状态映射，并以 `无变更` 取代误导性的未知数量。
- [ ] 14.8 为每种执行器增加崩溃/重启、重复、取消、重试、局部失败、检查点损坏、清理和载荷不含秘密等测试。
- [ ] 14.9 构建任务中心界面，主视图使用简短中文展示正在处理的内容、成功/失败数量和下一步操作，将错误码、检查点和诊断收纳到详情，并支持筛选、进度、重试/取消和终态自动刷新。

## 15. 安全、权限、审计与 MCP

- [ ] 15.1 扩展知识策略检查，使其覆盖资产、Office/HTML/SVG、OCR/视觉、分析、图谱、搜索、问答、MCP 和导出的读取、持久化、远程发送及输出阶段。
- [ ] 15.2 在 Rust 中实现精确路径根、符号链接、设备/UNC、Unicode、目标缺失时的父目录、防竞态创建和来源注册检查。
- [ ] 15.3 实现文件签名与压缩包限制、HTML/SVG 清洗、解析器禁宏/禁脚本/禁网络行为，以及临时目录边界测试。
- [ ] 15.4 为 Git 和 AI 复用安全凭证引用，并扫描代码、配置、日志、数据库行、界面输出、测试夹具和生成产物中的秘密泄漏。
- [ ] 15.5 对远程向量化、远程视觉/OCR 和远程对话/分析实施独立授权，并使用本机回环测试服务验证 HTTPS、主机策略、超时、重定向和响应大小限制。
- [ ] 15.6 为仓库配置、上传、删除/恢复、远程处理、分析确认、配置激活/回滚、关系确认、MCP 和问答引用增加脱敏审计。
- [ ] 15.7 MCP 知识工具默认保持只读，并为写入、确认或触发远程处理定义独立受控操作。
- [ ] 15.8 审查 Builder 插件、Capabilities、CSP、文件/对话框范围和开发 API 暴露，仅向目标窗口授予必要权限。
- [ ] 15.9 在本机执行允许与拒绝两类真实 Tauri 安全路径，使用回环测试服务覆盖远程传输，并验证任意路径、符号链接逃逸、恶意压缩包/HTML/SVG、未授权传输、敏感来源和 MCP Git 写入尝试。

## 16. React 知识工作台重构

- [ ] 16.1 引入知识库嵌套路由，将 `/knowledge` 保留为地址兼容入口并直接进入全新首次使用流程或项目工作台，不能重定向到旧交互页面。
- [ ] 16.2 提取项目上下文外壳，使用细粒度 Zustand 选择器管理所选项目、版本和仓库筛选条件，不持久化后端实体。
- [ ] 16.3 根据已确认的新用户流程从零构建目录、仓库、版本、文档、搜索、分析、图谱、问答、任务和设置路由级页面，各自拥有独立加载与错误边界；不得通过拆分现有单页组件来决定新页面结构。
- [ ] 16.4 使用 Ant Design 组件构建文档列表、详情、编辑、上传、版本、差异、删除和恢复流程，并提供稳定键、校验、乐观并发冲突处理和破坏性操作确认。
- [ ] 16.5 全面复用类型化领域 API 和 `getErrorMessage(error)`；移除组件内裸写的 `invoke` 及重复载荷组装。
- [ ] 16.6 在所有异步页面增加请求取消或仅保留最新请求保护、防重复提交、监听器清理和终态任务轮询。
- [ ] 16.7 所有新增下拉框、单选、多选、菜单、任务、来源、版本、解析器和图谱标签均使用中文，同时保持内部枚举、API 和数据库值稳定。
- [ ] 16.8 为工作台增加键盘操作、焦点返回、标签、提示/无障碍属性、缩放、小窗口、滚动归属、高对比度和 200% 缩放验收。
- [ ] 16.9 按新用户流程重写聚焦的路由与组件行为测试；旧单体页面测试仅用于识别业务回归点，不保留旧布局、步骤、控件位置或文案快照断言，且不得替代浏览器验收。
- [ ] 16.10 建立统一的渐进式配置组件模式：每步一个主要决策、默认折叠高级设置、提交前摘要、返回不丢失、提交中防重复和失败后可继续修改。
- [ ] 16.11 清理用户可见文案，使用短中文业务名称替代未解释的 `Profile`、`fingerprint`、`dimension`、`Commit SHA`、`checkpoint` 等术语，技术原文仅在“查看详情”中保留。
- [ ] 16.12 为首次进入、空项目、未配置向量、尚无文档和证据不足等状态提供一个明确主操作及不超过一段的简短说明，避免在主界面堆叠使用手册。
- [ ] 16.13 增加配置草稿保留、上一步/下一步、关闭恢复、默认值、字段级错误、焦点返回和高级设置展开状态的组件与页面测试。
- [ ] 16.14 以普通非技术用户任务完成为浏览器验收标准，验证其无需查阅技术文档或填写内部标识即可完成项目创建、文档上传、搜索、版本创建、向量启用和项目问答。
- [ ] 16.15 隔离旧知识页面与新路由：新页面不得导入旧业务交互组件或共享其页面状态；旧页面如需短期保留，只能位于独立回滚开关后且不得成为正常导航入口。

## 17. 兼容性、可观测性与发布控制

- [ ] 17.1 按阶段和项目实现新 UI 与新领域服务的独立功能开关；正常路由仅进入新 UI，紧急回滚可恢复旧兼容门面和隔离旧页面，但不得降低数据库版本。
- [ ] 17.2 为标题索引、解析器元数据、图谱投影和评测来源实现后台回填，持久化检查点并保持旧路径可用。
- [ ] 17.3 按项目、版本、仓库和类型生成文档覆盖报告，统计已发现、已解析、部分成功、已跳过、失败、已建立全文索引、已向量化和已进入图谱的文档。
- [ ] 17.4 实现检索与问答评测运行，记录数据集版本、范围、配置、排序配置、模型、环境、Recall@K、MRR、引用准确率、版本泄漏、拒答、冲突和延迟指标。
- [ ] 17.5 实现任务与资源指标，覆盖排队/运行时长、重试、失败阶段、吞吐量、数据库/资产增长、向量构建、图谱规模和查询 P50/P95。
- [ ] 17.6 根据本机技术验证证据定义量化发布与回滚阈值，并分别展示静态/单元检查和本机端到端验收结果。
- [ ] 17.7 验证每个领域迁移期间现有客户端、历史数据和 `/knowledge` 地址持续可用，但 `/knowledge` 的交互结构只遵循新 UI，且新写入内容仍能通过受支持的旧 API 读取。
- [ ] 17.8 新 UI 验收通过后从正常导航和默认路由移除旧页面；旧页面代码只在回滚窗口内隔离保留，后续与旧门面或冗余派生索引一起清理。

## 18. 验证、真实验收与交付

- [ ] 18.1 使用项目工具格式化所有已变更的 Rust、TypeScript、JSON 和 Markdown 文件，并验证文件为 UTF-8 无 BOM。
- [ ] 18.2 先运行聚焦的 Rust 单元、解析器、Database、迁移、Service、检索、图谱、向量、任务、安全和 IPC 测试，再扩展到相关 Rust 测试套件。
- [ ] 18.3 运行聚焦的 Vitest API、Store、组件和页面测试、TypeScript 无输出类型检查，以及前端生产构建。
- [ ] 18.4 运行 `cargo fmt --check`、相关 `cargo check`/clippy、生产 Tauri 构建或 macOS 打包检查，以及 `git diff --check`。
- [ ] 18.5 使用 Codex 内置浏览器或 Control Chrome 验证每个新路由、线性引导、默认值、高级设置、返回不丢失、简短文案、控制台、加载/空/错误/成功状态、表单校验、冲突、取消、重试、引用、图谱和小窗口流程；验收不得以旧 UI 的布局或操作步骤作为对照标准。
- [ ] 18.6 在本机分别通过真实 Tauri IPC 和浏览器开发 API 验证项目/仓库、上传、解析器、搜索、分析、图谱、向量、问答、任务和权限路径。
- [ ] 18.7 使用本机多仓库与文件夹具，以首次接触知识库的非技术用户视角执行全新流程：从 `/knowledge` 进入新引导，完成项目、仓库和版本配置，随后同步 Markdown/Office/图片/HTML、上传/编辑/提交文档、搜索、分析代码、确认文档、构建本地向量与图谱，并通过本地模型或测试 Provider 提出带引用的问题；全过程不得依赖旧页面入口或操作经验。
- [ ] 18.8 使用本机夹具验证同一流程覆盖损坏/不支持文件、脏仓库、Tag 缺失、中断/重试任务、未授权远程处理、敏感内容、无向量降级、证据冲突和证据不足拒答；远程路径使用回环测试服务。
- [ ] 18.9 在可恢复的数据库副本上验证当前有数据结构迁移、回填、功能回滚、旧 API 兼容、索引/配置回滚和来源事实保留。
- [ ] 18.10 在本机运行固定评测套件，确认解析覆盖率、搜索质量、引用准确率、版本泄漏、拒答、任务可靠性、安全和 P95 性能的发布阈值。
- [ ] 18.11 运行 `openspec validate "refactor-team-knowledge-base-platform" --type change --strict --no-interactive`，并在声明变更可归档前核对每项实施任务与已确认规格的一致性。
